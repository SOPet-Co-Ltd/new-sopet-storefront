# SOPET Storefront — Deploy to Vercel via Deploy Hook
# Triggers on push to deploy/production or deploy/uat.
# Configure VERCEL_DEPLOY_HOOK_URL in GitHub Environment secrets:
#   production → deploy/production branch hook
#   uat        → deploy/uat branch hook

name: Deploy

on:
  push:
    branches: [deploy/production, deploy/uat]
  workflow_dispatch:

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: ${{ github.ref_name == 'deploy/production' && 'deploy/production' || 'deploy/uat' }}

    steps:
      - name: Trigger Vercel deploy hook
        env:
          VERCEL_DEPLOY_HOOK_URL: ${{ secrets.VERCEL_DEPLOY_HOOK_URL }}
        run: |
          if [ -z "$VERCEL_DEPLOY_HOOK_URL" ]; then
            echo "::error::VERCEL_DEPLOY_HOOK_URL is not set for environment '${{ github.ref_name == 'deploy/production' && 'production' || 'uat' }}'"
            exit 1
          fi
          echo "Triggering Vercel deployment for branch ${{ github.ref_name }}..."
          curl -fsS -X POST "$VERCEL_DEPLOY_HOOK_URL"
          echo "Deploy hook triggered successfully."
