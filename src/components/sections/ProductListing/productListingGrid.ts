/** Fixed card widths (168px mobile, 224px md+) with columns that drop when space runs out. */
export const PRODUCT_LISTING_GRID_CLASS =
  'grid grid-cols-[repeat(auto-fill,168px)] justify-center gap-2 md:grid-cols-[repeat(auto-fill,224px)] md:gap-4 lg:gap-5';
