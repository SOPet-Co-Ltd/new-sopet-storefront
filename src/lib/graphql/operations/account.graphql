mutation RequestAccountDeletion {
  requestAccountDeletion
}

mutation ChangeCustomerPhone($input: ChangeCustomerPhoneInput!) {
  changeCustomerPhone(input: $input) {
    customer {
      id
      phone
      email
      fullName
    }
    pendingDeletion
    tokens {
      accessToken
      refreshToken
    }
  }
}
