fragment OrderConfirmationFields on OrderType {
  id
  orderNumber
  status
  createdAt
  subtotal
  shippingFee
  discountAmount
  total
  paymentMethod
  items {
    id
    variantId
    storeId
    productName
    unitPrice
    quantity
    subtotal
    fulfillmentStatus
  }
  storeShippings {
    storeId
    optionName
    shippingFee
  }
}

query Orders {
  orders {
    ...OrderConfirmationFields
  }
}

query Order($id: String!) {
  order(id: $id) {
    ...OrderConfirmationFields
  }
}
