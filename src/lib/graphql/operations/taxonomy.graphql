query ApprovedPetTypes {
  approvedPetTypes {
    id
    name
  }
}

query ApprovedBrands {
  approvedBrands {
    id
    name
  }
}

query ApprovedTags {
  approvedTags {
    id
    name
    slug
  }
}
